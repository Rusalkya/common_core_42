# URL de l'API backend (accessible depuis le navigateur)
# En local (sans Docker) :
# En local (sans Docker) :
# VITE_API_URL
# En Docker via nginx :
VITE_API_URL

# URL WebSocket backend
# VITE_WS_URL

VITE_APP_NAME