NODE_ENV
PORT
APP_NAME
API_PREFIX
API_VERSION

# === Frontend (CORS) ===
FRONTEND_URL
CORS_ORIGIN

# === Base de données PostgreSQL ===
# En local (sans Docker) :
# DATABASE_URL
# En Docker (décommenter si besoin) :
DATABASE_URL

# === JWT ===
# Génère un secret fort en prod : openssl rand -base64 64
JWT_SECRET
JWT_EXPIRES_IN
# === API publique (cle separee du JWT) ===
PUBLIC_API_KEY
